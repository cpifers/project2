source 'https://rubygems.org'

gem 'tk'        # Ruby binding for Tcl/Tk (Tcl/Tk 8.6 must be installed on the system)
gem 'csv'       # CSV export (no longer a default gem from Ruby 3.4)
gem 'minitest'  # tests

# Optional, only if you add PDF export later:
# gem 'prawn'
# gem 'prawn-table'
